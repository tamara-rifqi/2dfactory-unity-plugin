# 2dfactory-unity-plugin
Unity plugin for 2DFactory — automatically create and synchronize sprites, animation clips, Animator Controllers, prefabs, and socket data from 2DFactory sprite sheets and JSON metadata.
